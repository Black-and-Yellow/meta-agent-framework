global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "meta-agent"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["meta-agent:8080"]
        labels:
          service: "meta-agent"
