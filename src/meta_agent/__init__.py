"""
Meta-Agent: An LLM System that Designs, Builds, and Orchestrates Other LLM Agents.

Instead of writing pipelines by hand, the Meta-Agent reads a task,
invents the architecture, assembles the agents, runs the system,
and critiques its own design.

The code writes the code that solves the problem.
"""

__version__ = "0.1.0"
